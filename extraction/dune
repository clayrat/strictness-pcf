; checker.ml / checker.mli are generated by `coqc -Q ../theories PCF Extract.v`.
; -w -a silences the warnings that Coq-extracted OCaml routinely trips.
(executable
 (name main)
 (modules checker main)
 (flags
  (:standard -w -a)))
